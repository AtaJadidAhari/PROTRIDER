# path to output directory to store result files
out_dir: output

# RNA-seq count matrix. Columns are samples and rows are genes.
input_intensities: "samples/data/drop_demo_counts_all.tsv"

# Column containing gene IDs.
index_col: "geneID"
input_format: "genes_as_rows"

## Preprocessing params
analysis: "outrider"
fpkmCutoff: 1
gtf: "samples/data/gencode_annotation_trunc.gtf"
outrider_precision: "float32" # float32 or float64
outrider_early_stopping: false # optional; monitors full-cohort NLL after theta fitting
outrider_early_stopping_patience: 5
outrider_early_stopping_min_delta: 1e-5
outrider_early_stopping_min_epochs: 10

# seed parameter for reproducibility
seed: 42

## Grid search outlier injection params
inj_freq: 1e-3
inj_mean: 3
inj_sd: 1.6
gs_epochs: 100

## Model params
# TODO: improve error when set to False and trying to plot trainng_loss
autoencoder_training: True # when set to false, it returns values after (PCA) initialization without training
autoencoder_loss: "NLL" # MSE | NLL | BBL

n_layers: 1 # number of layers for the autoencoder model
n_epochs: 1 # number of training epochs

# TODO: try with higher LR
lr: 1e-4 # learning rate
batch_size:

find_q_method: "OHT" # OUTRIDER supports OHT or an integer value
init_pca: True # PCA initialization
h_dim: # hidden dimension, only for n_layers>1

## Stat params
pval_dist: 'nb'
pval_adj: 'by' # Multiple testing correction. Options: by, bh, holm
pval_sided: 'two-sided'
calculate_one_sided_pval: False
pseudocount: 0.01 # for FC computation (and jaccard)

## Reporting params
outlier_threshold: 0.1 # for adjusted p values 
report_all: True 

verbose: False
device: 'cpu'

n_jobs: 8
